data/raw/
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
.env

# NBA image assets — managed by the standalone headshot fetcher (D:\NBA Assets by
# default, outside this repo). Never commit binaries if the path is ever set inside a repo.
NBA Assets/
PlayerHeadshots/

# Claude Design export (rendered artifact, not source)
/Hack-a-Shaq Venn.html